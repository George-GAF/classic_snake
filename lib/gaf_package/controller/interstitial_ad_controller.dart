


class InterstitialAdController {
  /*
   static late  InterstitialAd _interstitialAd;

  static bool _isInterstitialAdReady = false;

  static void _loadInterstitialAd() async {
    await _interstitialAd.load();
  }

  static void _onInterstitialAdEvent(MobileAdEvent event) {
    switch (event) {
      case MobileAdEvent.loaded:
        _isInterstitialAdReady = true;
        break;
      case MobileAdEvent.failedToLoad:
        _isInterstitialAdReady = false;
        break;
      default:
      // do nothing
    }
  }

  static void loadInterstitialAd() async {
    _interstitialAd = InterstitialAd(
      adUnitId: AdManager.interstitialAdUnitId,
      listener: _onInterstitialAdEvent,
    );
    _loadInterstitialAd();
  }

  static Future<bool> showInterstitialAd() async {
    bool show = false;
    if (_isInterstitialAdReady) show = await _interstitialAd.show();
    return show;
  }
  */
}
